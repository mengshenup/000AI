import os

# ---------------------------------------------------------------- #
#  用户数据目录()
#
#  变量用处：
#     定义存放用户生成数据（如截图、日志）的文件夹路径。
#
#  易懂解释：
#     这是服务器的“档案室”。所有的照片和记录都存在这里。
#
#  警告：
#     如果修改此路径，请确保新路径有读写权限。
# ---------------------------------------------------------------- #
# 设置用户数据存储目录的相对路径
USER_DATA_DIR = "./user_data"

# ---------------------------------------------------------------- #
#  浏览器视口配置()
#
#  变量用处：
#     定义 Playwright 浏览器的默认分辨率。
#
#  易懂解释：
#     给机器人配的“显示器”大小。这里设成了 720P。
# ---------------------------------------------------------------- #
# 定义浏览器窗口的宽度和高度
VIEWPORT = {'width': 1280, 'height': 720}

# ---------------------------------------------------------------- #
#  默认搜索链接()
#
#  变量用处：
#     当没有指定 URL 时，浏览器默认打开的页面。
#
#  易懂解释：
#     机器人的“主页”。一打开浏览器就去这里。
# ---------------------------------------------------------------- #
# 定义默认的目标搜索 URL
TARGET_SEARCH_URL = "https://www.douyin.com/search/三角洲行动_零号大坝_老六点位"

# ---------------------------------------------------------------- #
#  计费配置表()
#
#  变量用处：
#     定义不同 AI 模型和网络流量的单价，用于计算成本。
#
#  易懂解释：
#     这是“价目表”。用一次 AI 要花多少钱，都在这里写着。
#
#  警告：
#     单位通常是 美元/百万Token。请根据实际 API 价格调整。
# ---------------------------------------------------------------- #
# 定义计费规则字典
PRICING_TABLE = {
    "gemini-1.5-flash": {
        "input": 0.075,  # 输入 Token 单价 (每百万 Token)
        "output": 0.30   # 输出 Token 单价 (每百万 Token)
    },
    "network_egress": 0.1  # 网络流出流量单价 ($0.10 per GB)
}

# ---------------------------------------------------------------- #
#  初始化目录()
#
#  代码用处：
#     在模块加载时检查并创建用户数据目录。
#
#  易懂解释：
#     如果发现没有“档案室”，就赶紧建一个。
# ---------------------------------------------------------------- #
# 检查用户数据目录是否存在
if not os.path.exists(USER_DATA_DIR):
    # 如果不存在，则创建该目录
    os.makedirs(USER_DATA_DIR)